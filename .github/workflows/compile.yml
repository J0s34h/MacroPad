name: Compile Arduino Sketch (macOS arm64)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  compile:
    runs-on: self-hosted
    env:
      RUNNER_TOOL_CACHE: /Users/gh-runner/hostedtoolcache

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create Python tool cache directory
        run: mkdir -p "$RUNNER_TOOL_CACHE"

      - name: Install arduino-cli
        run: |
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | sh
          mkdir -p "$HOME/.local/bin"
          mv bin/arduino-cli "$HOME/.local/bin/"
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Install Python 3.11 with Homebrew
        run: |
          brew update
          brew install python@3.11
          echo "/opt/homebrew/bin" >> $GITHUB_PATH

      # ----- Create and activate a virtual environment -----
      - name: Create virtual environment
        run: python3 -m venv venv

      - name: Activate venv and set as default Python
        run: |
          echo "VIRTUAL_ENV=$PWD/venv" >> $GITHUB_ENV
          echo "$PWD/venv/bin" >> $GITHUB_PATH
      # -----------------------------------------------------

      - name: Install pyyaml
        run: |
          pip install --upgrade pip
          pip install pyyaml

      # Extract the profile name from sketch.yaml (default_profile or fallback to 'release')
      - name: Get profile name
        id: profile
        run: |
          python3 <<EOF
          import yaml
          with open('sketch.yaml') as f:
              data = yaml.safe_load(f)
          profile = data.get('default_profile', 'release')
          with open('$GITHUB_OUTPUT', 'a') as f:
              f.write(f"name={profile}\n")
          EOF

      - name: Compile sketch (using profile)
        env:
          ARDUINO_DIRECTORIES_DATA: ${{ runner.temp }}/arduino-data
        run: |
          echo "Compiling with profile: ${{ steps.profile.outputs.name }}"
          arduino-cli compile --profile "${{ steps.profile.outputs.name }}" .

      - name: List installed cores (for debugging)
        if: always()
        run: arduino-cli core list

      - name: List boards matching FQBN (for debugging)
        if: always()
        run: arduino-cli board listall | grep -i "rp2040" || true